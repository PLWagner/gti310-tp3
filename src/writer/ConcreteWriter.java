/*
package writer;
import java.io.*;

public class ConcreteWriter implements Writer {

	@Override
										//HLinkedMap
	public void write(String filename, Object output) {
		
        try {
        BufferedWriter out = new BufferedWriter(new FileWriter(filename));
		//BufferedReader br = new BufferedReader(new FileReader((String) output)); //TODO
		String strLine;
		
		while ((strLine = liste.hasNext()) != null) { //TODO
                out.write(output + "\n"); //TODO output.getQqch()
            }
            out.close();
        } catch (IOException e) {}
		
	}

}
*/
